# publish the test rule to local repository
> rules / publish

# without the proper resolver/repository, invocation of the rule fails
-> example / scalafix RenameToFoo

# once the proper resolver is setup via sbt, dependency fetching works fine and scalafix can be used normally
> set ThisBuild / resolvers += Resolver.file("custom-ivy", (rules / target).value / "custom-ivy")(Resolver.ivyStylePatterns)
-> example / scalafix --check RenameToFoo
> example / scalafix RenameToFoo
> example / scalafix --check RenameToFoo
